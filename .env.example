# Canonical origin of the deployed site. No trailing slash.
#
# The app already defaults to the live domain, so you only need this for
# preview deploys or a staging host — set it there so previews do not claim
# the production canonical URL and get themselves indexed.
#
# NEXT_PUBLIC_SITE_URL=https://staging.manimaranmahesan.com
