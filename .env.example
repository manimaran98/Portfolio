# The canonical origin of the deployed site.
# Every canonical link, sitemap entry and social-card URL is built from this,
# so setting it wrong points search engines at the wrong host.
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
